// A Java file — a language aiscan cannot analyse. Its presence must surface as
// a partial-coverage finding so the record never over-claims coverage.
package com.example;

public class Report {
    public String generate(String input) {
        return "report: " + input;
    }
}
